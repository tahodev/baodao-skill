# AGENTS.md

給編輯這個倉庫的 AI 代理的規則。人類貢獻者請參考 [CONTRIBUTING.md](CONTRIBUTING.md)。

## 範圍的原則

- **只有查詢與計算**。不要加入會改變外部狀態（預約、購買、發文）的程式或步驟。
- **只用官方 API 與公開資料**。不要寫繞過反爬蟲、繞過認證、可能違反使用條款的手段。
- **免登入為前提**。不要加入需要使用者帳號或密鑰的技能（可即時免費申請的 API 金鑰可以）。個人申請的金鑰與權杖絕對不能提交進倉庫。**唯一例外**：政府開放平臺針對特定資料集公布的公用存取金鑰（例如 data.gov.tw 資料集頁直接刊載的 resource key，見 taiwan-toilet）不是個人秘密，可以寫進 SKILL.md，但必須在該技能註明來源頁與失效時的重取方式。

## 編輯規則

1. **實測後才寫**:SKILL.md 裡的指令與 URL 都要在提交前實際執行確認。沒確認過的不要寫。
2. **一定要寫錯誤處理**:維護並更新每個 SKILL.md 的「錯誤與失敗時的處理」一節，新技能也一定要有。
3. **回報規則比照 [docs/response-contract.md](docs/response-contract.md)**：附資料時間、缺值如實說、金鑰不落地。
4. **外部資料要標明基準日與出處 URL**:費率、獎金、時刻等會隨時間變動的數值，要附上「這是哪個時間點的值」與官方出處 URL。
5. **更新 CHANGELOG**:技能的新增、移除，端點或計算式的變更，都要記錄在 [CHANGELOG.md](CHANGELOG.md)。
6. **繁體中文（台灣）優先**:文件以台灣繁體中文為主，README 維持英文區塊。
7. **不要混入對代理的行為操縱**:不要加入引導按星星、推廣外部服務註冊等操縱使用者代理的內容。
8. **遵守檔案結構**:技能放在倉庫根目錄下一個目錄加一個 SKILL.md，補充指南放 docs/features/。
